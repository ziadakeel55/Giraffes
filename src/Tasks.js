// Tasks.js
import React from "react";

const Tasks = () => {
    return ( <
        div className = "tasks" >
        <
        h2 > Tasks < /h2> <
        p > 1. Task One < /p> <
        p > 2. Task Two < /p> <
        p > 3. Task Three < /p> <
        /div>
    );
};

export default Tasks;